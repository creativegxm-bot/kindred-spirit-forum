import { SimpleTricks } from "./SimpleTricks";
const Bridge = () => <SimpleTricks title="Bridge (lite)" trumpSuit="random" cardsPerHand={13} description="Lite version — random trump, no bidding or contracts." />;
export default Bridge;
